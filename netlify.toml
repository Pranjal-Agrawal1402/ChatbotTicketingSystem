[build]
  command = "pip install -r requirements.txt -t netlify/functions/"
  functions = "netlify/functions"
  publish = "static"

[functions]
  included_files = ["templates/**", "static/**"]

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/app"
  status = 200
